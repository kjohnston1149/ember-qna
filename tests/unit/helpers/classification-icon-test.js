
import { classificationIcon } from 'ember-qna/helpers/classification-icon';
import { module, test } from 'qunit';

module('Unit | Helper | classification icon');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = classificationIcon([42]);
  assert.ok(result);
});
